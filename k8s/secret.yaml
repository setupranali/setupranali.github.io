# =============================================================================
# SetuPranali - Secret (TEMPLATE)
# =============================================================================
# IMPORTANT: This is a TEMPLATE. Do NOT commit real secrets!
#
# For production:
# 1. Use external secret management (AWS Secrets Manager, Vault, etc.)
# 2. Or create secrets via kubectl:
#    kubectl create secret generic ubi-connector-secrets \
#      --namespace=ubi-connector \
#      --from-literal=UBI_SECRET_KEY=<your-key> \
#      --from-literal=REDIS_URL=<your-redis-url>
#
# Generate encryption key:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: ubi-connector-secrets
  namespace: ubi-connector
  labels:
    app.kubernetes.io/name: ubi-connector
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # -----------------------------------------------------------------------------
  # REQUIRED: Replace these with real values
  # -----------------------------------------------------------------------------
  
  # Fernet encryption key (32-byte base64-encoded)
  # Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
  UBI_SECRET_KEY: "REPLACE_WITH_REAL_KEY"
  
  # Redis connection URL
  # Format: redis://[password@]host:port/db
  REDIS_URL: "redis://redis:6379/0"

